import { createTheme } from '@mui/material/styles';

const customTheme = createTheme({
  palette: {
    dark: {
      main: '#363636',
      contrastText: '#F5F5F5',
    },
  },
});

export { customTheme };
